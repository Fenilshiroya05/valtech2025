spring:
  application:
    name: ASSIGNMENT-LOAN
  
  datasource:
    driver-class: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/assloan
    username: postgres
    password: postgres
 
  jpa :  
    hibernate:
      ddl-auto: update
